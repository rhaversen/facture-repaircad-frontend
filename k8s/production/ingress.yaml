apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: frontend-ingress
    namespace: facture-repaircad-production
    annotations:
        cert-manager.io/cluster-issuer: letsencrypt-production
spec:
    ingressClassName: nginx
    tls:
        -   hosts:
                - repaircad.facture.live
            secretName: frontend-tls
    rules:
        -   host: repaircad.facture.live
            http:
                paths:
                    -   backend:
                            service:
                                name: frontend-service
                                port:
                                    number: 80
                        path: /
                        pathType: Prefix
